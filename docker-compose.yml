services:
  portfolio:
    image: ghcr.io/alexdalat/portfolio:latest
    ports:
      - "${PORT_BIND:-3000}:3000"
    volumes:
      - ./public/Resume/:/app/public/Resume/:ro
      - /home/alex/syncthing/Personal/Work/Resume/:/home/alex/syncthing/Personal/Work/Resume/:ro
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    labels:
      com.centurylinklabs.watchtower.enable: "true"
